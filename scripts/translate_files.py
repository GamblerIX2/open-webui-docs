#!/usr/bin/env python3
"""批量写入翻译后的文档文件"""
import os

BASE = pathlib.Path(__file__).resolve().parents[1] / "docs"

files = {}

files["features/administration/analytics/index.mdx"] = '''---
sidebar_position: 1050
title: "Analytics（分析）"
---

# Analytics（分析）

Open WebUI 中的 **Analytics** 功能为管理员提供了关于实例使用模式、Token 消耗和模型性能的全面洞察。这一强大工具帮助您了解用户与 AI 模型的交互方式，并基于数据做出资源分配和模型选择决策。

:::info 仅管理员可用
Analytics 仅供拥有 **admin** 角色的用户访问。通过 **管理面板 > Analytics** 进入。
:::

:::tip 禁用 Analytics

如果您不需要 Analytics 标签，可以通过将 [`ENABLE_ADMIN_ANALYTICS`](/reference/env-configuration#enable_admin_analytics) 环境变量设置为 `False` 来完全隐藏它。这会阻止挂载分析 API 路由，并从管理员导航中移除该标签。此更改需要重启才能生效。

:::

## 概述

Analytics 仪表板为您提供 Open WebUI 实例活动的全局视图，包括：

- 不同模型和时间段的**消息量**
- 用于费用估算和资源规划的 **Token 使用量**跟踪
- 了解参与度的**用户活动**模式
- 展示小时、天或月趋势的**时间序列数据**

所有分析数据均来源于实例数据库中存储的消息历史记录。启用 Analytics 功能后，Open WebUI 会自动跟踪和索引消息，以提供快速、可查询的洞察。

---

## 访问 Analytics

1. 使用 **admin** 账户登录
2. 导航到 **管理面板**（点击个人资料图标 → 管理面板）
3. 点击管理员导航中的 **Analytics** 标签

---

## 仪表板功能

### 时间段选择

在 Analytics 仪表板右上角，您可以按时间段筛选所有数据：

- **最近 24 小时** - 实时监控的小时粒度
- **最近 7 天** - 过去一周的每日概览
- **最近 30 天** - 月度快照
- **最近 90 天** - 季度趋势
- **全部时间** - 完整历史数据

您选择的时间段会**自动保存**，并在浏览器会话之间持久存储。

### 用户组过滤

如果您配置了[用户组](/features/authentication-access/rbac/groups)，Analytics 仪表板支持按组过滤：

- 使用时间段选择器旁边的**组下拉菜单**
- 选择特定组，仅查看**该组用户**的分析数据
- 选择"所有用户"查看全实例分析

适用场景：
- **部门级报告** - 跟踪特定团队的使用情况
- **成本分摊** - 将 Token 消耗归因于业务单元
- **试点项目** - 监控测试组的推广情况

更改时间段或组过滤器时，页面上的所有指标会自动更新。

### 汇总统计

仪表板标题显示所选时间段的关键指标：

- **总消息数** - 生成的 AI 助手响应数量
- **总 Token 数** - 处理的所有输入和输出 Token 之和
- **总对话数** - 独立对话的数量
- **总用户数** - 发送过消息的用户数量

:::note 消息计数
Analytics 统计的是 **AI 助手响应**而非用户消息。这能更准确地衡量 AI 模型使用情况和 Token 消耗。
:::

### 消息时间线图表

交互式时间线图表按模型分类展示消息量随时间的变化。主要功能：

- **小时或每日粒度** - 根据所选时间段自动调整
- **多模型可视化** - 最多显示 8 个具有不同颜色的模型
- **悬停提示** - 显示任意时间点各模型的精确计数和百分比
- **趋势识别** - 快速发现使用模式、高峰时段和模型推广情况

此图表帮助您：
- 识别容量规划的繁忙时段
- 跟踪部署后的模型推广情况
- 检测异常活动峰值
- 监控变更或公告的影响

### 模型使用表格

每个模型使用情况的详细分类：

| 列 | 说明 |
|--------|-------------|
| **#** | 按消息数排名 |
| **模型** | 带图标的模型名称 |
| **消息数** | 生成的 AI 助手响应总数 |
| **Token 数** | 消耗的总 Token 数（输入 + 输出） |
| **%** | 占总消息数的百分比 |

**功能：**
- **可排序列** - 点击列标题按名称或消息数排序
- **模型图标** - 通过头像图片进行视觉识别
- **Token 跟踪** - 查看哪些模型消耗资源最多
- **可点击行** - 点击任意模型打开[模型详情模态框](#模型详情模态框)

**使用场景：**
- 识别最受欢迎的模型
- 计算每个模型的费用（将 Token 数乘以提供商价格）
- 决定保留或移除哪些模型
- 根据使用情况规划基础设施升级

### 模型详情模态框

点击任意模型行即可打开包含两个标签的详细模态框：

#### 概览标签

概览标签提供：

- **反馈活动图表** - 用户反馈（点赞/踩）随时间变化的可视化历史记录
  - 在 **30 天**、**1 年**或**全部时间**视图之间切换
  - 较长时间范围使用周粒度聚合
- **标签** - 与此模型关联的最常见对话标签（前 10 个）

这有助于您了解：
- 用户对模型质量的感知随时间的变化
- 模型处理的主题/使用场景
- 用户满意度趋势

#### 对话标签

:::info 需要管理员对话访问权限
只有在实例设置中启用 [**`ENABLE_ADMIN_CHAT_ACCESS`**](/reference/env-configuration#enable_admin_chat_access) 时，对话标签才可见。
:::

对话标签显示使用此模型的对话：

- **用户信息** - 发起每个对话的用户
- **预览** - 每个对话的第一条消息
- **时间戳** - 对话最后更新的时间
- **点击打开** - 直接导航到共享对话视图

适用于：
- 了解用户如何与特定模型交互
- 审计模型使用情况以确保质量
- 寻找用于培训或文档的示例对话

### 用户活动表格

跟踪每个用户的参与度和 Token 消耗：

| 列 | 说明 |
|--------|-------------|
| **#** | 按活动排名 |
| **用户** | 带头像的用户名 |
| **消息数** | 该用户发送的总消息数 |
| **Token 数** | 该用户消耗的总 Token 数 |

**功能：**
- **可排序列** - 按名称或活动级别整理
- **用户识别** - 头像和显示名称
- **Token 归因** - 查看每个用户的资源消耗

**使用场景：**
- 监控高级用户及其 Token 消耗
- 识别非活跃或低使用率账户
- 规划用户配额或速率限制
- 计算账单用途的每用户费用

---

## Token 使用量跟踪

### 什么是 Token？

Token 是语言模型处理文本的单位。**输入**（您的提示词）和**输出**（模型的响应）都消耗 Token。大多数 AI 提供商按 Token 使用量收费，因此 Token 跟踪对于费用管理至关重要。

### Token 跟踪的工作原理

Open WebUI 自动从模型响应中捕获 Token 使用量，并与每条消息一起存储。Analytics 功能汇总这些数据以显示：

- **输入 Token** - 用户提示词和上下文中的 Token
- **输出 Token** - 模型响应中的 Token
- **总 Token** - 输入和输出之和

Token 数据在不同模型提供商（OpenAI、Ollama、llama.cpp 等）之间进行标准化，无论使用哪个后端都能提供一致的指标。

### Token 使用量指标

**Token 使用量**部分提供：

- **按模型的 Token 明细** - 每个模型的输入、输出和总 Token
- **总 Token 消耗** - 全实例 Token 使用量
- **每消息 Token 数** - 用于效率分析的每消息 Token 数

:::tip 费用估算
要估算费用，将 Token 数乘以您的提供商定价：
```
费用 = (输入_token 数 × 输入单价) + (输出_token 数 × 输出单价)
```

GPT-4 示例：
- 输入：1,000,000 token × $0.03/1K = $30
- 输出：500,000 token × $0.06/1K = $30
- **合计：$60**
:::

---

## 使用场景

### 1. 资源规划

**场景：** 您为团队运行 Open WebUI，需要规划基础设施容量。

**Analytics 的帮助：**
- 查看**消息时间线**识别高峰使用时段
- 检查**模型使用**了解哪些模型需要更多资源
- 监控 **Token 使用量**估算未来费用
- 跟踪**用户活动**为团队增长做规划

### 2. 模型评估

**场景：** 您已部署多个模型，想了解用户更喜欢哪些。

**Analytics 的帮助：**
- 比较各模型的**消息数**查看推广率
- 检查 **Token 效率**（每消息 Token 数）识别冗长模型
- 引入新模型后在时间线图表中监控**趋势**
- 结合 [Evaluation 功能](/features/administration/evaluation/)获取质量洞察

### 3. 费用管理

**场景：** 您使用付费 API 提供商，需要控制费用。

**Analytics 的帮助：**
- 按模型和用户跟踪**总 Token 消耗**
- 识别**高使用量用户**以进行配额讨论
- 比较不同模型提供商的 **Token 费用**
- 使用时间段过滤器定期审查

### 4. 用户参与

**场景：** 您想了解团队如何使用 AI 工具。

**Analytics 的帮助：**
- 监控**活跃用户**与注册账户的对比
- 识别可能需要支持或培训的**高级用户**
- 随时间跟踪**推广趋势**
- 将使用情况与团队计划或培训活动关联

### 5. 合规与审计

**场景：** 您的组织需要使用报告以满足合规要求。

**Analytics 的帮助：**
- 生成特定时间段的**活动报告**
- 跟踪所有 AI 交互的**用户归因**
- 监控已批准与未批准模型的**模型使用**
- 通过 API 导出数据用于外部报告工具

---

## 技术详情

### 数据存储

Analytics 数据存储在 `chat_message` 表中，包含：

- **消息内容** - 用户和 AI 助手的消息
- **元数据** - 模型 ID、用户 ID、时间戳
- **Token 使用量** - 输入、输出和总 Token
- **关联关系** - 与父消息和对话的链接

启用 Analytics（通过迁移）时，Open WebUI：
1. 创建带有优化索引的 `chat_message` 表
2. **回填**您对话历史中的现有消息
3. **双写**新消息到对话 JSON 和消息表

这种双写方式确保：
- **向后兼容** - 现有功能继续正常工作
- **快速查询** - Analytics 不影响对话性能
- **数据一致性** - 所有消息均被捕获

### 数据库索引

以下索引优化分析查询：

- `chat_id` - 快速查找对话中的所有消息
- `user_id` - 快速用户活动报告
- `model_id` - 高效的模型使用查询
- `created_at` - 时间范围过滤
- 常用查询模式的复合索引

### API 端点

对于高级用户和集成，Analytics 提供 REST API 端点：

**仪表板端点：**
```
GET /api/v1/analytics/summary
GET /api/v1/analytics/models
GET /api/v1/analytics/users
GET /api/v1/analytics/messages
GET /api/v1/analytics/daily
GET /api/v1/analytics/tokens
```

**模型详情端点：**
```
GET /api/v1/analytics/models/{model_id}/chats    # 获取使用此模型的对话
GET /api/v1/analytics/models/{model_id}/overview # 获取反馈历史和标签
```

**常用查询参数：**

| 参数 | 类型 | 说明 |
|-----------|------|-------------|
| `start_date` | int | Unix 时间戳（秒）- 范围开始 |
| `end_date` | int | Unix 时间戳（秒）- 范围结束 |
| `group_id` | string | 过滤到特定用户组（可选） |

:::tip API 访问
所有 Analytics 端点均需要管理员身份验证。包含您的管理员 Bearer 令牌：
```bash
curl -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \\
  "https://your-instance.com/api/v1/analytics/summary?group_id=abc123"
```
:::

---

## 隐私与数据注意事项

### 哪些数据会被跟踪？

Analytics 跟踪：
- ✅ 消息时间戳和计数
- ✅ 每条消息的 Token 使用量
- ✅ 模型 ID 和用户 ID
'''

files["features/administration/evaluation/index.mdx"] = '''---
sidebar_position: 1100
title: "Evaluation（评估）"
---

## 为什么应该评估模型？

认识一下 **Alex**——一家中型公司的机器学习工程师。Alex 知道市面上有众多 AI 模型——GPT、LLaMA 等等——但哪个最适合手头的工作？它们在纸面上听起来都很出色，但 Alex 不能仅凭公开的排行榜来决策。这些模型在不同场景下的表现各异，而且某些模型可能已经在评估数据集上进行了训练（不诚实！）。此外，这些模型的写作风格有时……感觉不对劲。

这就是 Open WebUI 的用武之地。它为 Alex 及其团队提供了一种简单的方式，根据实际需求来评估模型。无需复杂的数学计算，无需繁重的操作。只需在与模型交互时点赞或踩。

### 简而言之

- **为什么评估很重要**：模型众多，但并非所有模型都适合您的特定需求。公开的通用排行榜并不总是可信的。
- **如何解决这个问题**：Open WebUI 提供内置的评估系统。使用点赞/踩对模型响应评分。
- **幕后发生了什么**：评分会调整您的个性化排行榜，已评分对话的快照将用于未来的模型微调！
- **评估选项**：
  - **竞技场模式**：随机为您选择模型进行比较。
  - **普通交互**：像平常一样聊天并对响应评分。

---

### 为什么公开评估还不够？

- 公开的排行榜并不针对**您的**特定使用场景。
- 某些模型在评估数据集上进行了训练，影响了结果的公正性。
- 一个模型整体表现可能很好，但其沟通风格或响应内容与您想要的"感觉"不符。

### 解决方案：使用 Open WebUI 进行个性化评估

Open WebUI 有一个内置的评估功能，让您和您的团队在与模型交互的过程中，发现最适合您特定需求的模型。

怎么运作的？很简单！

- **在对话过程中**，如果您喜欢某个响应就点赞，不喜欢就点踩。如果消息有**兄弟消息**（例如重新生成的响应或并排模型比较的一部分），您就在为您的**个人排行榜**做贡献。
- **排行榜**可在管理面板中轻松访问，帮助您跟踪哪些模型根据团队的评价表现最佳。

有一个很酷的功能？**每当您对响应评分时**，系统会**捕获该对话的快照**，这些快照后续将用于精炼模型，甚至为未来的模型训练提供数据。（请注意，此功能仍在开发中！）

---

### 评估 AI 模型的两种方式

Open WebUI 提供两种直接的 AI 模型评估方法。

### **1. 竞技场模式**

**竞技场模式**从可用模型池中随机选择，确保评估公平无偏。这有助于消除手动比较中的一个潜在缺陷：**生态有效性**——确保您不会有意或无意地偏向某个模型。

使用方法：
- 从竞技场模式选择器中选择一个模型。
- 像平常一样使用，但现在您处于"竞技场模式"。

要让您的反馈影响排行榜，您需要所谓的**兄弟消息**。什么是兄弟消息？兄弟消息就是由同一查询生成的任何替代响应（想想消息重新生成或多个模型并排生成响应）。这样，您就在**正面对决**地比较响应。

- **评分技巧**：当您为一个响应点赞时，另一个会自动获得踩。因此要谨慎，只为您真正认为最好的消息点赞！
- 对响应评分后，您可以查看排行榜，了解各模型的排名情况。

下面是竞技场模式界面的预览：

![竞技场模式示例](/images/evaluation/arena.png)

想要更深入的体验？您甚至可以复制 [**Chatbot Arena**](https://lmarena.ai/) 风格的设置！

![Chatbot Arena 示例](/images/evaluation/arena-many.png)

### **2. 普通交互**

如果您不想切换到"竞技场模式"，完全没问题。您可以像平常一样使用 Open WebUI，并在日常操作中对 AI 模型响应评分。随时对模型响应点赞/踩即可。但是，**如果您希望您的反馈用于排行榜排名**，您需要**切换模型并与不同的模型交互**。这确保存在**兄弟响应**可供比较——只有两个不同模型之间的比较才会影响排名。

例如，这是在普通交互中如何评分：

![普通模型评分界面](/images/evaluation/normal.png)

以及如何设置多模型比较，类似于竞技场：

![多模型比较](/images/evaluation/normal-many.png)

---

## 排行榜

评分后，查看**管理面板**下的**排行榜**。这里您将直观地看到模型的表现，使用 **Elo 评分系统**（类似国际象棋评级！）进行排名。您将真实地看到哪些模型在评估中真正脱颖而出。

这是一个排行榜布局示例：

![排行榜示例](/images/evaluation/leaderboard.png)

### 模型活动跟踪

除了总体 Elo 评分外，您现在还可以通过**模型活动图表**查看模型的历史表现。此功能提供了模型评估随时间演变的时间顺序视图。

- **分叉图表**：图表按日或按周显示胜利（正）和失败（负），清楚地展示模型随时间的可靠性。
- **时间范围**：您可以在不同时间范围之间切换：**30 天**、**1 年**或**全部时间**。
- **周聚合**：对于较长的时间范围（1 年和全部时间），数据会自动按周聚合，以提供更平滑、更易读的趋势。

要查看活动图表，请点击排行榜中的某个模型，打开其详细评估模态框。

对对话评分时，您可以**按主题打标签**以获得更细粒度的洞察。如果您在**客户服务、创意写作、技术支持**等不同领域工作，这尤其有用。

#### 自动标签
Open WebUI 会尝试根据对话主题**自动为对话打标签**。但是，根据您使用的模型，自动标签功能有时可能**失败**或误解对话。发生这种情况时，最好**手动为对话打标签**以确保反馈准确。

- **如何手动打标签**：对响应评分时，您可以根据对话的上下文添加自己的标签。

不要跳过这一步！打标签非常强大，因为它让您能够**根据特定主题重新排名模型**。例如，您可能想查看哪个模型在回答技术支持问题方面表现最好，而不是一般客户咨询。

以下是重新排名的示例：

![按主题重新排名排行榜](/images/evaluation/leaderboard-reranked.png)

---

### 附注：用于模型微调的对话快照

每当您对模型响应评分时，Open WebUI 都会*捕获该对话的快照*。这些快照最终可用于**微调您自己的模型**——因此您的评估会推动 AI 的持续改进。

*（请继续关注此功能的更多更新，它正在积极开发中！）*

---

## 总结

**简而言之**，Open WebUI 的评估系统有两个明确目标：
1. 帮助您**轻松比较模型**。
2. 最终找到与您个人需求最契合的模型。

该系统的核心是让 AI 模型评估对每个用户来说都**简单、透明且可定制**。无论是通过竞技场模式还是普通对话交互，**您掌控着决定哪个 AI 模型最适合您特定使用场景的权力**！

默认情况下，所有评估数据都保留在**您的实例**上，除非您特别**选择加入社区共享**，否则不会分享任何数据。
'''

files["features/authentication-access/index.mdx"] = '''---
sidebar_position: 0
title: "认证与访问控制"
---

# 🔐 认证与访问控制

**控制谁可以登录、他们能做什么，以及您的实例如何与身份体系集成。**

Open WebUI 从第一天起就支持多用户。无论您是在运行个人实例还是跨组织管理数千个席位，您都可以完全控制认证、授权和程序化访问。连接您的身份提供商、定义细粒度权限、自动化用户生命周期管理——无需修改一行代码。

---

## 本节内容

| | |
| :--- | :--- |
| 👥 **[RBAC](./rbac)** | 角色、用户组和按资源设置的权限。定义谁可以访问什么 |
| 🔑 **[SSO / OIDC](./auth/sso)** | 使用 Google、Microsoft、Okta、Keycloak 或任何 OIDC 提供商进行联合认证 |
| 📂 **[LDAP](./auth/ldap)** | 对您现有的目录服务进行认证 |
| 📋 **[SCIM 2.0](./auth/scim)** | 从您的 IdP 自动化用户和用户组的预配与取消预配 |
| 🔐 **[API Keys](./api-keys)** | 用于脚本、机器人、流水线和集成的程序化访问 |

---

## 整体架构

```mermaid
flowchart LR
    IdP["身份提供商<br/>(Google、Okta、LDAP...)"] -->|SSO / OIDC / LDAP| Auth["认证"]
    IdP -->|SCIM 2.0| Provision["用户 & 用户组<br/>预配"]
    Auth --> RBAC["RBAC 引擎"]
    Provision --> RBAC
    RBAC -->|角色 + 用户组| Permissions["按用户<br/>权限"]
    Permissions --> Resources["模型 · 知识库<br/>工具 · 频道"]
    APIKeys["API Keys"] -->|Bearer Token| RBAC
```

1. **用户通过** SSO、OIDC、LDAP 或本地凭据**进行认证**。
2. **SCIM 从身份提供商自动预配**用户和用户组。
3. **RBAC 根据**角色（管理员、用户）和用户组成员资格**确定访问权限**。
4. **权限是累加的**：每个用户组成员资格都会增加能力，而不会减少。
5. **API Keys** 继承创建用户的完整权限，用于程序化访问。

:::tip 以 SSO 为中心的用户体验
在仅 SSO 的环境中，您可以通过 `ENABLE_PASSWORD_AUTH=false` 禁用本地密码认证，并通过 `ENABLE_PASSWORD_CHANGE_FORM=false` 隐藏账户页面中的密码更新控件。
:::

---

## 快速开始

### 个人或小团队？从简单开始

Open WebUI 开箱即用，支持本地邮箱/密码认证。不需要外部身份提供商。创建账户、分配管理员或用户角色，即可完成。

### 组织？分层引入 SSO 和 SCIM

1. **[设置 SSO](./auth/sso)**：配置您的身份提供商以实现单点登录
2. **[配置 RBAC](./rbac)**：创建用户组、分配权限并设置访问控制列表
3. **[启用 SCIM](./auth/scim)** *（可选）*：从您的 IdP 自动化用户生命周期
4. **[生成 API Keys](./api-keys)** *（可选）*：为自动化启用程序化访问

---

## 核心概念

### 累加权限

Open WebUI 的安全模型是**累加**的。用户从其角色的默认权限开始，用户组成员资格只会*增加*能力。用户的有效权限是其角色和所有用户组授予的所有权限的并集。

### 按资源访问控制

模型、知识库、工具和 Skills 都支持细粒度访问控制。资源**默认为私有**。创建者通过用户授权、用户组授权或公开可见性来控制谁可以查看和使用它们。

### 管理员与用户

有两种主要角色。**管理员**拥有完整的系统访问权限。**用户**的能力由默认权限加上用户组成员资格定义。第三种角色**待审批**，将新注册用户放入队列等待管理员批准。

[**角色 →**](./rbac/roles) · [**权限 →**](./rbac/permissions) · [**用户组 →**](./rbac/groups)
'''

files["features/authentication-access/api-keys.md"] = '''---
sidebar_position: 500
title: "API Keys"
---

# 🔑 API Keys

**Open WebUI 的程序化访问——适用于脚本、机器人和集成。**

API Keys 是个人访问令牌，允许外部代码调用 Web 界面使用的相同端点。您在浏览器中能做的任何事情——对话补全、模型列举、文件上传、RAG 查询——您的脚本都可以通过单个 `Authorization: Bearer` 请求头来完成。每个密钥继承创建者的权限，因此无需学习单独的权限模型。

---

## 为什么使用 API Keys？

### 无需浏览器的自动化

脚本、CI/CD 流水线、监控机器人和第三方工具都需要程序化访问。API Keys 为它们提供了稳定的凭证，不会随浏览器会话过期。

### 相同权限，不同接口

API Key 代表您行事。它继承您的角色和用户组权限——管理 Web 界面的相同访问控制也适用于每个 API 请求。

### 可撤销且可审计

为每个密钥起一个描述性名称（例如"CI 流水线"、"监控机器人"）以跟踪使用情况。如果密钥泄露，立即删除——无需重置密码、无需使会话失效，只需单击一次。

---

## 主要功能

| | |
| :--- | :--- |
| 🔐 **Bearer token 认证** | 标准 `Authorization: Bearer` 请求头，适用于任何 HTTP 客户端或 SDK |
| 🛡️ **限定于用户范围** | 密钥继承创建用户的角色和用户组权限 |
| 🚫 **端点限制** | 可选择限制密钥可访问的 API 路由 |
| 👥 **权限门控** | 非管理员用户需要全局管理员开关加上每用户组的功能权限 |

---

## 入门指南

### 第一步：全局启用 API Keys（管理员操作）

1. 以**管理员**身份登录
2. 打开 **管理面板 > 设置 > 通用**
3. 滚动到 **认证** 部分
4. 切换 **启用 API Keys** 为开
5. 点击 **保存**

:::info
这是全局主开关。关闭时，任何人——包括管理员——都无法生成密钥。开启时：
- **管理员**用户可以立即生成密钥
- **非管理员**用户仍需要 API Keys 功能权限（第二步）
:::

*（可选）* 启用 **API Key 端点限制**，以限制 API Keys 可调用的路由。将允许的端点指定为逗号分隔的列表（例如 `/api/v1/models,/api/v1/chat/completions`）。

### 第二步：向非管理员用户授予权限（管理员操作）

非管理员用户需要 **API Keys** 功能权限。使用以下任一方式授予：

#### 方式 A：默认权限（所有用户）

1. **管理面板 > 用户 > 用户组 > 默认权限**
2. 在 **功能权限** 下，切换 **API Keys** 为开
3. 点击 **保存**

:::warning
这会授予所有具有"user"角色的用户生成 API Keys 的能力。要进行更严格的控制，请使用方式 B。
:::

#### 方式 B：用户组（特定用户）

1. **管理面板 > 用户 > 用户组**
2. 选择或创建一个用户组（例如"API 用户"）
3. 在 **权限 > 功能权限** 下，切换 **API Keys** 为开
4. 点击 **保存**

:::tip
创建一个专用的"API 用户"或"监控"用户组，只将需要程序化访问的账户添加进去。这遵循最小权限原则。
:::

### 第三步：生成密钥

1. 点击**个人资料图标**（左下角侧边栏）
2. 选择 **设置 > 账户**
3. 在 **API Keys** 部分，点击 **生成新 API Key**
4. 为其起一个描述性名称（例如"监控机器人"）
5. **立即复制密钥**——您以后将无法再次查看它

:::warning
将 API Keys 视为密码。将其存储在密钥管理器中，永远不要将其提交到版本控制，也不要在公共频道中分享。如果密钥泄露，立即删除并生成新的。
:::

---

## 使用您的 API Key

在 `Authorization` 请求头中将密钥作为 Bearer token 传递：

```bash
curl -H "Authorization: Bearer YOUR_API_KEY" \\
  http://localhost:8080/api/models
```

```python
import requests
import pathlib

response = requests.get(
    "http://localhost:8080/api/models",
    headers={"Authorization": "Bearer YOUR_API_KEY"}
)
print(response.json())
```

有关完整的端点参考——对话补全、Ollama 代理、RAG、文件管理等——请参阅 [API 端点](/reference/api-endpoints)。

---

## 最佳实践

### 专用服务账户

专门为自动化创建一个**非管理员用户**（例如 `monitoring-bot`、`ci-pipeline`），从该账户生成密钥。如果密钥泄露，攻击者只能获得该用户的权限——而不是管理员访问权限。

### 端点限制

启用 **API Key 端点限制**，只将您的集成实际需要的路由加入白名单。监控机器人只需要 `/api/models` 和 `/api/chat/completions`——不要给它访问 `/api/v1/files/` 或管理员端点的权限。

### 密钥轮换

定期删除旧密钥并生成新密钥，特别是对于长期运行的集成。为密钥起名时加上日期或版本以跟踪轮换（"监控机器人 - 2025-Q1"）。

---

## 故障排查

**在设置 > 账户中看不到 API Keys 部分？**

- **检查全局开关：** 确认管理员已在 **管理面板 > 设置 > 通用 > 启用 API Keys** 中启用 API Keys。参见 [`ENABLE_API_KEYS`](/reference/env-configuration#enable_api_keys)。
- **检查您的权限（非管理员用户）：** 确认您的账户或用户组在 **功能权限** 下具有 **API Keys** 功能权限。参见 [`USER_PERMISSIONS_FEATURES_API_KEYS`](/reference/env-configuration#user_permissions_features_api_keys)。

**收到 `401 Unauthorized` 响应？**

- 验证密钥格式是否正确：`Authorization: Bearer sk-...`
- 检查密钥是否已被删除
- 如果启用了端点限制，确认您调用的路由在允许列表中

---
'''

files["features/authentication-access/rbac/index.mdx"] = '''---
sidebar_position: 100
title: "基于角色的访问控制（RBAC）"
---

Open WebUI 实现了一个灵活且安全的**基于角色的访问控制（RBAC）**系统。该系统允许管理员通过三个相互关联的层次来精确管理用户能力和资源访问：

1.  [**角色**](./roles.md)：高级用户类型（管理员、用户、待审批）。这定义了基线信任级别。
2.  [**权限**](./permissions.md)：细粒度功能标志（例如"可以删除对话"、"可以使用网络搜索"）。
3.  [**用户组**](./groups.md)：用于组织用户、授予额外权限以及管理对资源共享访问（ACL）的机制。资源也可以直接共享给单个用户。

:::info 核心概念：累加权限
安全模型是**累加**的。用户从其默认权限开始，用户组成员资格会**增加**能力。用户实际拥有的权限是其角色和用户组授予的所有权限的*并集*。
:::

:::warning RBAC 与提供商凭证的范围边界
RBAC 控制用户在 **Open WebUI 内部**能做什么（功能、资源和 UI/API 操作）。

RBAC **不**替代外部提供商的最小权限配置。如果您连接了 OpenAI 兼容的代理/提供商（例如 LiteLLM、OpenRouter 或自定义网关），请使用在您的部署中用于推理的最小所需范围的提供商凭证。

除非您的部署明确需要该级别的信任，否则请避免为一般用户流量配置管理/主密钥。
:::

## 文档指南

*   [‍🔑 **角色**](./roles.md)
    *   了解管理员和用户之间的区别。
    *   了解管理员的限制及安全/隐私配置。

*   [🔒 **权限**](./permissions.md)
    *   探索完整的可用权限开关列表。
    *   了解对话、工作区和功能的细粒度控制。
    *   **安全提示**：了解正确配置全局默认值如何保护您的系统。

*   [‍🔐 **用户组**](./groups.md)
    *   了解如何构建团队和项目。
    *   **策略**：区分"权限用户组"（用于权限）和"共享用户组"（用于访问）。
    *   管理私有模型和知识库的访问控制列表（ACL）——与用户组或单个用户共享。
'''

for rel_path, content in files.items():
    full_path = os.path.join(BASE, rel_path)
    os.makedirs(os.path.dirname(full_path), exist_ok=True)
    with open(full_path, 'w', encoding='utf-8') as f:
        f.write(content)
    print(f"Written: {rel_path}")
