#!/usr/bin/env python3
"""批量写入翻译后的 RBAC 和 auth 文件"""
import os, pathlib

BASE = pathlib.Path("d:/Github/open-webui-docs/docs")

def w(rel, content):
    p = BASE / rel
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text(content, encoding="utf-8")
    print(f"Written: {rel}")

# ─── roles.md ──────────────────────────────────────────────────────────────
w("features/authentication-access/rbac/roles.md", '''---
sidebar_position: 3
title: "角色"
---


Open WebUI 定义了三种主要的**系统角色**，用于确定用户账户的基础访问级别。这些角色与[用户组](./groups.md)和[权限](./permissions.md)有所不同。

| 角色 | 关键字 | 说明 |
| :--- | :--- | :--- |
| **管理员** | `admin` | **超级用户**。对系统拥有完全控制权。 |
| **用户** | `user` | **标准用户**。受 RBAC 权限约束。 |
| **待审批** | `pending` | **受限**。在获得批准之前无法访问。 |

## 角色详情

### 管理员（`admin`）
管理员角色专为系统维护人员设计。
*   **完全访问**：默认访问所有资源和设置。
*   **管理权限**：可以管理用户、用户组和全局配置。
*   **绕过检查**：默认情况下绕过大多数权限检查。

:::warning 管理员限制与最佳实践
虽然管理员通常拥有不受限制的访问权限，但某些系统配置可能会出于安全和隐私原因限制其能力：
*   **隐私控制**：像 `ENABLE_ADMIN_CHAT_ACCESS=False` 这样的环境变量可以防止管理员查看用户的对话。
*   **特定功能异常可能存在**：某些功能可以在标准管理员绕过行为之外强制执行额外检查。特别是对于 API Keys，只要启用了 `ENABLE_API_KEYS`，管理员就可以生成密钥。
*   **访问控制例外**：如果禁用了 `BYPASS_ADMIN_ACCESS_CONTROL`，管理员可能需要明确权限才能访问私有模型/知识库/笔记资源。

为获得稳健的安全态势，我们建议通过用户组将管理员纳入您的权限体系，而不仅仅依赖角色的隐式绕过。这确保了在启用绕过限制时能保持一致的访问。
:::

### 标准用户（`user`）
这是团队成员的默认功能角色。
*   **受权限约束**：没有隐式访问权限。所有能力必须通过**全局默认权限**或**用户组成员资格**来授予。
*   **累加权限**：如[权限](./permissions.md)部分所述，其有效权限是所有授予的总和。

### 待审批用户（`pending`）
新注册用户（如果已配置）或已停用用户的默认状态。
*   **零访问**：无法执行任何操作或查看任何内容。
*   **需要审批**：必须由现有管理员提升为 `user` 或 `admin`。

:::note
`admin` 角色对所有内容实际上都有 `check_permission() == True`。细粒度权限（如禁用"网络搜索"）通常**不**适用于管理员。
:::

## 角色分配

### 初始设置
*   **第一个用户：** 在全新安装中创建的第一个账户会自动被分配**管理员**角色。
*   **无头管理员创建：** 对于自动化/容器化部署，可以使用环境变量自动创建管理员账户（见下文）。
*   **后续用户：** 新注册用户被分配**默认用户角色**。

### 配置
可以通过 `DEFAULT_USER_ROLE` 环境变量控制新用户的默认角色：

```bash
DEFAULT_USER_ROLE=pending
# 选项：\'pending\'、\'user\'、\'admin\'
```
*   **pending（推荐用于公共/共享实例）：** 新用户在管理员于管理面板中明确激活之前无法登录。
*   **user：** 新用户以默认权限立即获得访问权限。
*   **admin：**（谨慎使用）新用户成为完全管理员。

## 更改角色
管理员可以随时通过**管理面板 > 用户**更改用户的角色。
*   将用户提升为 `admin` 会授予其完全控制权。
*   将管理员降级为 `user` 会使其再次受到权限系统约束。

## 无头管理员账户创建

对于**自动化部署**（Docker、Kubernetes、云平台），手动交互不切实际时，Open WebUI 支持在首次启动时使用环境变量自动创建管理员账户。

### 工作原理

配置以下环境变量时：
- `WEBUI_ADMIN_EMAIL`：管理员账户的邮箱地址
- `WEBUI_ADMIN_PASSWORD`：管理员账户的密码
- `WEBUI_ADMIN_NAME`：（可选）管理员显示名称（默认为"Admin"）

Open WebUI 将自动：
1. 在启动时检查数据库中是否存在任何用户
2. 如果数据库为空（全新安装），使用提供的凭据创建管理员账户
3. 安全地对密码进行哈希处理并存储
4. 自动禁用注册（`ENABLE_SIGNUP=False`）以防止未经授权的账户创建

### 使用场景

此功能在以下场景特别有用：
- **CI/CD 流水线**：使用密钥管理系统中的凭据自动预配 Open WebUI 实例
- **Docker/Kubernetes 部署**：消除容器启动和手动创建管理员之间的时间间隔
- **自动化测试**：创建具有预配置管理员账户的可重现测试环境
- **无头服务器**：部署无法通过 UI 手动创建账户的实例

### 配置示例

#### Docker Compose
```yaml
services:
  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    environment:
      - WEBUI_ADMIN_EMAIL=admin@example.com
      - WEBUI_ADMIN_PASSWORD=${ADMIN_PASSWORD}  # 使用密钥管理
      - WEBUI_ADMIN_NAME=System Administrator
    # ... 其他配置
```

#### Kubernetes Secret
```yaml
apiVersion: v1
kind: Secret
metadata:
  name: openwebui-admin
type: Opaque
stringData:
  admin-email: admin@example.com
  admin-password: your-secure-password
  admin-name: System Administrator
---
apiVersion: v1
kind: Pod
metadata:
  name: open-webui
spec:
  containers:
  - name: open-webui
    image: ghcr.io/open-webui/open-webui:main
    env:
    - name: WEBUI_ADMIN_EMAIL
      valueFrom:
        secretKeyRef:
          name: openwebui-admin
          key: admin-email
    - name: WEBUI_ADMIN_PASSWORD
      valueFrom:
        secretKeyRef:
          name: openwebui-admin
          key: admin-password
    - name: WEBUI_ADMIN_NAME
      valueFrom:
        secretKeyRef:
          name: openwebui-admin
          key: admin-name
```

### 重要说明

:::warning 安全注意事项
- **使用密钥管理**：切勿在 Docker Compose 文件或 Dockerfile 中硬编码 `WEBUI_ADMIN_PASSWORD`。请使用 Docker secrets、Kubernetes secrets 或环境变量注入。
- **强密码**：在生产部署中使用强且唯一的密码。
- **设置后更改**：考虑在初始部署后通过 UI 更改管理员密码以提高安全性。
- **自动禁用注册**：创建管理员账户后，注册将自动禁用。如果需要，可以稍后通过**管理面板 > 设置 > 通用**重新启用。
:::

:::info 行为详情
- **仅在全新安装时**：管理员账户**仅**在数据库中没有用户时创建。如果用户已存在，这些环境变量将被忽略。
- **密码哈希**：密码使用与手动账户创建相同的机制进行安全哈希，确保安全性。
- **一次性操作**：这是首次启动时的一次性操作。使用相同环境变量的后续重启不会修改现有管理员账户。
:::

有关这些环境变量的完整文档，请参阅[环境配置指南](/reference/env-configuration#webui_admin_email)。
''')

# ─── permissions.md ────────────────────────────────────────────────────────
w("features/authentication-access/rbac/permissions.md", '''---
sidebar_position: 3
title: "权限"
---


Open WebUI 提供了一个灵活的权限系统，允许管理员为用户配置访问控制和功能可用性。这实现了对用户在应用程序中可以访问和修改的内容的细粒度控制。

管理员可以通过两种主要方式管理权限：
1.  **默认权限：** 通过**管理面板 > 用户 > 用户组 > 默认权限**设置适用于所有用户（包括管理员）的基础权限。
2.  **用户组权限：** 通过**管理面板 > 用户 > 用户组**创建具有特定权限覆盖的用户组（例如，拥有图像生成访问权限的"高级用户"组）。

拥有 `Pending` 角色的用户在获得批准之前没有访问权限，`Admin` 用户拥有完整的管理员访问权限，而 `User` 账户受下面描述的权限系统约束。

:::warning RBAC 范围边界
RBAC 权限控制用户在 Open WebUI 内部能做什么。

RBAC 不替代提供商端的最小权限设计。对于 OpenAI 兼容的提供商/代理（包括 LiteLLM），请使用部署所需最小范围的凭据进行配置。
:::

:::info 权限逻辑
Open WebUI 中的权限是**累加**的。
*   用户的有效权限是**全局默认值**和其所有**用户组成员资格**的组合。
*   **True 优先于 False**：如果*任何*来源（全局默认值或*任何*单个用户组）授予了某个权限，用户**将**拥有该权限。
*   **不存在"拒绝"功能**：您不能使用特定用户组来"取消"另一个用户组或全局默认值授予的权限。要限制某个功能，必须在全局默认值*和*用户所属的**所有**用户组中将其禁用。
:::

:::tip 最佳实践：最小权限原则
由于权限是**累加**的，推荐的安全策略是从**限制**开始：
1.  **最小化全局默认权限**：将默认权限（**管理面板 > 用户 > 用户组 > 默认权限**）配置为仅包含*所有用户绝对必须*拥有的内容。
2.  **通过用户组授权**：创建特定用户组（例如"创作者"、"高级用户"）来明确**授予**高级功能，如图像生成或文件上传。

这种方式确保新用户不会意外获得敏感功能的访问权限，同时允许您通过简单地将用户添加到相关用户组来轻松提升权限。
:::

## 权限类别

权限分为五个主要类别：**工作区**、**共享**、**对话**、**功能**和**设置**。

### 1. 工作区权限
控制对"工作区"部分的访问，用户在此创建和管理资源。
某些权限**依赖**于其他权限（例如，如果无法访问模型工作区，就无法导入模型）。

| 权限 | 说明 |
| :--- | :--- |
| **模型访问** | **（父级）** 访问**模型**工作区以创建或编辑自定义模型。 |
| **模型导入** | *（需要模型访问）* 从 JSON/文件导入模型的能力。 |
| **模型导出** | *（需要模型访问）* 将模型导出到文件的能力。 |
| **知识库访问** | 访问**知识库**工作区以管理知识库。 |
| **提示词访问** | **（父级）** 访问**提示词**工作区以管理自定义系统提示词。 |
| **提示词导入** | *（需要提示词访问）* 导入提示词的能力。 |
| **提示词导出** | *（需要提示词访问）* 导出提示词的能力。 |
| **工具访问** | **（父级）** 访问**工具**工作区以管理函数/工具。 |
| **工具导入** | *（需要工具访问）* 导入工具的能力。 |
| **工具导出** | *（需要工具访问）* 导出工具的能力。 |

:::danger ⚠️ 工具访问 = 等同 Root 级别的访问权限
**将工具访问权限视为等同 root 的访问权限。** 授予用户创建或导入工具的访问权限等同于给予其服务器的 shell 访问权限，因为工具和函数会执行任意 Python 代码。只将此权限授予您信任能直接访问服务器的用户。如果您为不受信任的用户启用此权限，则是在接受在主机上任意执行代码的风险。有关完整详情，请参阅[插件安全警告](/features/extensibility/plugin/)。
:::

| **技能访问** | 访问**技能**工作区以创建和管理可复用指令集。 |

### 2. 共享权限
控制用户可以与社区共享或公开的内容。

| 权限 | 说明 |
| :--- | :--- |
| **共享模型** | **（父级）** 共享模型（使其他人可访问）的能力。 |
| **公开模型** | *（需要共享模型）* 使模型可公开发现的能力。 |
| **共享知识库** | **（父级）** 共享知识库的能力。 |
| **公开知识库** | *（需要共享知识库）* 使知识库公开的能力。 |
| **共享提示词** | **（父级）** 共享提示词的能力。 |
| **公开提示词** | *（需要共享提示词）* 使提示词公开的能力。 |
| **共享工具** | **（父级）** 共享工具的能力。 |
| **公开工具** | *（需要共享工具）* 使工具公开的能力。 |
| **共享技能** | **（父级）** 共享技能的能力。 |
| **公开技能** | *（需要共享技能）* 使技能公开的能力。 |
| **共享笔记** | **（父级）** 共享笔记的能力。 |
| **公开笔记** | *（需要共享笔记）* 使笔记公开的能力。 |

### 3. 对话权限
控制对话界面中用户可用的功能。

| 权限 | 说明 |
| :--- | :--- |
| **对话控制** | **（父级）** 访问高级对话设置。Valves、系统提示词和参数所必需。 |
| **模型 Valves** | *（需要对话控制）* 访问模型特定的配置"valves"。 |
| **系统提示词** | *（需要对话控制）* 编辑对话系统提示词的能力。 |
| **参数** | *（需要对话控制）* 调整 LLM 参数（如温度、top_k）的能力。 |
| **文件上传** | 向对话上传文件的能力。 |
| **删除对话** | 删除整个对话的能力。 |
| **删除消息** | 删除单条消息的能力。 |
| **编辑消息** | 编辑消息的能力。 |
| **继续响应** | 对截断的响应使用"继续"功能的能力。 |
| **重新生成响应** | 重新生成 AI 响应的能力。 |
| **评价响应** | 为响应点赞/踩的能力。 |
| **分享对话** | 生成对话分享链接的能力。 |
| **导出对话** | 导出对话历史的能力。 |
| **语音转文字（STT）** | 使用语音输入的能力。 |
| **文字转语音（TTS）** | 使用语音输出的能力。 |
| **音频通话** | 使用实时音频通话功能的能力。 |
| **多模型** | 选择多个模型进行同步响应的能力。 |
| **临时对话** | **（父级）** 切换"临时对话"（隐身模式/关闭历史记录）的能力。**注意：** 为保护隐私，此模式下后端文档解析被禁用。 |
| **强制临时** | *（需要临时对话）* **限制**用户*始终*使用临时对话（禁用历史记录）。 |

### 4. 功能权限
控制对广泛平台能力的访问。

| 权限 | 说明 |
| :--- | :--- |
| **API Keys** | 非管理员用户在用户设置中生成个人访问令牌（API Keys）的能力。 |
| **笔记** | 访问"笔记"功能。 |
| **频道** | 访问"频道"功能。 |
| **文件夹** | 使用文件夹整理对话的能力。 |
| **网络搜索** | 使用网络搜索集成的能力。 |
| **图像生成** | 使用图像生成工具的能力。 |
| **代码解释器** | 使用 Python 代码解释器的能力。 |
| **直连工具服务器** | 在设置中连接自定义工具服务器的能力。 |
| **记忆** | 访问用于持久用户上下文的记忆功能。 |
| **自动化** | 非管理员用户访问自动化页面并创建、编辑、运行、暂停或删除自己的定时自动化的能力。 |
| **日历** | 访问日历功能，用于创建日历、管理事件和查看共享日历。 |

:::info 自动化权限范围

对于自动化访问：

1. **非管理员的权限检查**：拥有 `user` 角色的用户需要 **功能 > 自动化**（`features.automations`）权限。
2. **管理员免于 `features.automations`**：拥有 `admin` 角色的用户无需该特定权限即可访问自动化。

默认权限可通过 [`USER_PERMISSIONS_FEATURES_AUTOMATIONS`](/reference/env-configuration#user_permissions_features_automations) 进行配置。

:::

:::info 日历权限范围

对于日历访问：

1. **非管理员的权限检查**：拥有 `user` 角色的用户需要 **功能 > 日历**（`features.calendar`）权限。
2. **管理员免于 `features.calendar`**：拥有 `admin` 角色的用户无需该特定权限即可访问日历。

默认权限可通过 [`USER_PERMISSIONS_FEATURES_CALENDAR`](/reference/env-configuration#user_permissions_features_calendar) 进行配置。

:::

### 5. 设置权限
控制对用户设置区域的访问。

| 权限 | 说明 |
| :--- | :--- |
| **界面设置访问** | 在用户设置中访问和修改界面设置的能力。 |

:::info API Keys 权限范围

对于 API Key 创建：

1.  **需要全局开关**：必须在**管理设置 > 通用 > 启用 API Keys** 中全局启用该功能。如果此项关闭，*任何人*都无法生成密钥。
2.  **非管理员的权限检查**：拥有 `user` 角色的用户必须具有 `features.api_keys` 权限。
3.  **管理员免于 `features.api_keys`**：当 API Keys 全局启用时，拥有 `admin` 角色的用户可以生成 API Keys，无需该特定权限。

:::

:::tip 最佳实践：创建管理员用户组

**为应对未来的权限变更，请为管理员创建专用用户组：**

1.  通过**管理面板 > 用户 > 用户组****创建"管理员"用户组**
2.  将所有管理员用户**添加到此用户组**
3.  根据需要向该用户组**授予用户可见功能权限**（例如，为非管理员组成员授予 API Keys 权限）

这种方式确保当新增的权限适用于管理员时，您可以通过用户组轻松授予所有管理员，而无需修改各个用户设置。它还提供了对哪些管理员可以访问哪些功能的细粒度控制。

:::

## 环境变量

虽然推荐通过 UI 管理权限，但初始默认值可以通过环境变量设置。这些变量通常以 `USER_PERMISSIONS_` 为前缀。
*   `ENABLE_IMAGE_GENERATION=True`
*   `ENABLE_WEB_SEARCH=True`
*   `USER_PERMISSIONS_CHAT_FILE_UPLOAD=True`

有关有效变量的完整列表，请参阅[环境配置](/reference/env-configuration)指南。
''')

# ─── groups.md ─────────────────────────────────────────────────────────────
w("features/authentication-access/rbac/groups.md", '''---
sidebar_position: 3
title: "用户组"
---


Open WebUI 中的用户组是大规模组织用户和管理访问控制的强大机制。它们有两个主要用途：
1.  **权限管理：** 高效地向多个用户分配细粒度权限。
2.  **资源访问控制：** 控制谁可以访问特定的私有资源（模型、知识库、工具）。

:::info 权限合并逻辑
Open WebUI 权限是**累加**的（基于并集）。
*   如果用户属于多个用户组，他们会获得所有权限的**并集**。
*   如果*用户组 A* 允许"图像生成"而*用户组 B* 不允许，同时属于这两个用户组的用户**将**拥有图像生成访问权限。
*   "拒绝"权限不存在；您只能"授予"权限。
:::

## 用户组管理

用户组可以在**管理面板 > 用户组**部分进行管理。

### 用户组配置
创建或编辑用户组时，您可以配置其在系统中的可见性：

*   **谁可以向此用户组共享**：（访问控制设置）
    *   **任何人**：（默认）平台上的任何用户都可以在"访问控制"菜单中看到此用户组，并可向其共享对话内容、模型、提示词或知识库。
    *   **成员**：只有**已经是**该用户组成员的用户才会在"访问控制"菜单中看到它作为选项。这是私人团队协作（例如"市场营销"团队）的理想设置，确保只有队友可以相互共享资源（模型、提示词、知识库）。
    *   **无人**：该用户组对非管理员用户完全**隐藏**于共享菜单中。这非常适合专门用于 **RBAC 权限分配**的技术用户组（例如"高级用户"组），不需要内容共享功能。

:::tip 策略：权限用户组 vs 共享用户组
为了维护一个干净且易管理的系统，考虑使用命名方案将用户组分为两类：

1.  **权限用户组**（例如前缀 `[权限]`、`角色-` 或 `P-`）
    *   **目的**：专门用于授予功能（例如 `[权限] 图像生成`、`[权限] 网络搜索`）。
    *   **配置**：将"谁可以共享"设置为**无人**。
    *   **结果**：用户获得所需功能，但这些技术用户组不会影响"共享"菜单的简洁性。

2.  **共享用户组**（例如前缀 `团队-`、`项目-` 或普通名称）
    *   **目的**：专门用于组织人员（例如 `市场营销`、`工程`、`团队 Alpha`）以共享资源。
    *   **配置**：将"谁可以共享"设置为**成员**或**任何人**。
    *   **最佳实践**：**禁用这些用户组中的所有权限**。
        *   依赖*全局默认权限*（或单独的*权限用户组*）来获得功能权限。
        *   *为什么？* 这确保了轻松的**权限撤销**。如果您决定全局禁用某个功能（例如"网络搜索"），它将立即对所有人生效。如果您的共享用户组启用了"网络搜索"，您将需要手动更新每个用户组来移除该权限，因为用户组的 `True` 状态会覆盖全局的 `False`。保持功能用户组简洁以维护全局控制。
:::

### 创建方式
*   **手动创建：** 管理员可以通过 UI 手动创建用户组并添加用户。
*   **OAuth 同步：** 如果启用了 `ENABLE_OAUTH_GROUP_MANAGEMENT`，用户组可以从您的 OAuth 提供商（例如 Keycloak、Azure AD）同步。
    *   **自动创建：** 启用 `ENABLE_OAUTH_GROUP_CREATION` 后，本地不存在的用户组将自动创建。
    *   **成员同步：** 用户会严格按照 OAuth claims 添加/移除到用户组。

### 用户组结构
用户组定义通常包括：
*   **名称**：用户组的显示名称。
*   **描述**：用户组的用途说明。
*   **权限**：覆盖默认用户权限的详细 JSON 对象（参见[权限](./permissions.md)）。
*   **成员**：属于该用户组的用户 ID 列表。

## 向用户组分配权限

编辑用户组时，您可以切换特定权限。
*   **默认状态**：默认情况下，用户组不授予*额外*权限；成员依赖全局默认值。
*   **授权访问**：将权限（例如"网络搜索"）切换为**开**意味着所有成员都能获得该功能，即使全局禁用了它。

## 资源访问（RBAC）

您可以使用用户组或单个用户授权来限制对特定对象（如专有模型或敏感知识库）的访问。

1.  **标记资源**：创建/编辑模型或知识库时，将其可见性设置为**私有**或**受限**。
2.  **授予访问**：选择应具有"读取"或"写入"访问权限的特定**用户组**或**单个用户**。重新设计的访问控制 UI 使同时添加多个用户组或用户变得非常方便。

:::tip 模型的知识库范围
除可见性外，知识库访问也受模型配置限制。当模型附加了**特定知识库**时，它只能访问这些 KB（而非所有用户可访问的 KB）。详情请参阅[使用原生函数调用的知识库范围](/features/workspace/knowledge#scoped-access-keeps-things-organized)。
:::

### 访问授权系统
在更深层面，资源访问通过数据库中存储的标准化**访问授权**进行管理。每个授权指定：

*   **资源**：资源的类型和 ID（例如特定模型或知识库）。
*   **主体**：谁获得访问权限——**用户组**或**单个用户**。
*   **权限**：访问级别——`read`（读取）或 `write`（写入）。

例如，授予"市场营销"用户组对某模型的读取访问权限，以及授予特定编辑用户写入访问权限，将创建两个独立的授权条目。公开访问由具有通配符（`*`）主体的用户授权表示。

*   **读取**：用户可以查看和使用资源。
*   **写入**：用户可以更新或删除资源。
''')

print("RBAC files done")
